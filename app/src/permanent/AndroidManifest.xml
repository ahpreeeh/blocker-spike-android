<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- La V0 permanente utilise la suspension Device Owner officielle. -->
    <uses-permission
        android:name="android.permission.SYSTEM_ALERT_WINDOW"
        tools:node="remove" />

    <application>
        <!-- The permanent Device Owner still needs the neutral T3 request
             screen. Only the legacy accessibility interception is removed. -->
        <service
            android:name="com.albugimed.blockerspike.service.BlockerAccessibilityService"
            tools:node="remove" />
    </application>
</manifest>
